<%= render partial: 'productsearchform' %>

<p>Baked Goods and Other Carbs: <% @carbs_search.each do |b|%>
	<p><%= b.name %></p>
	<% end %>
</p>

<%= render partial: 'random_celebrity_sighting/random_celebrity_sighting', locals: {action_name: 'rotate_image'} %>
